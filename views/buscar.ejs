<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resultados de la Búsqueda</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <style>
        body {
            background-color: #000; /* Fondo negro */
            color: #fff; /* Texto blanco */
        }
        h1 {
            margin-bottom: 40px; /* Más separación del título */
        }
        .card {
            background-color: #1a1a1a; /* Fondo oscuro para las tarjetas */
            border: none;
            height: 100%; /* Asegura que todas las tarjetas tengan la misma altura */
        }
        .card-title, .card-text {
            color: #fff; /* Texto blanco dentro de las tarjetas */
        }
        .card-img-top {
            height: 200px; /* Altura fija para las imágenes */
            object-fit: cover; /* Recorta las imágenes para que se ajusten al contenedor */
        }
        .row {
            display: flex;
            flex-wrap: wrap; /* Permite que las tarjetas se ajusten en filas */
            gap: 20px; /* Espaciado entre las tarjetas */
        }
        .col-md-4 {
            flex: 1 1 calc(33.333% - 20px); /* Tres tarjetas por fila con espacio entre ellas */
            max-width: calc(33.333% - 20px);
        }
        @media (max-width: 768px) {
            .col-md-4 {
                flex: 1 1 calc(50% - 20px); /* Dos tarjetas por fila en pantallas pequeñas */
                max-width: calc(50% - 20px);
            }
        }
        @media (max-width: 576px) {
            .col-md-4 {
                flex: 1 1 100%; /* Una tarjeta por fila en pantallas muy pequeñas */
                max-width: 100%;
            }
        }
    </style>
    <!-- Barra de navegación -->
    <%- include('partials/navbar') %>

    <div class="container mt-5">
        <h1 class="text-center">Resultados de la Búsqueda</h1>
        <p class="text-center">Resultados para: "<strong><%= query %></strong>"</p>

        <% if (productos.length > 0) { %>
            <div class="row">
                <% productos.forEach(producto => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <img src="<%= producto.imagen %>" class="card-img-top" alt="<%= producto.nombre %>">
                            <div class="card-body">
                                <h5 class="card-title"><%= producto.nombre %></h5>
                                <p class="card-text">$<%= producto.precio %></p>
                                <a href="/compradores/productos/<%= producto.id %>" class="btn btn-primary">Ver Producto</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p class="text-center text-muted">No se encontraron productos que coincidan con tu búsqueda.</p>
        <% } %>
    </div>

    <footer class="bg-primary text-white text-center py-3 mt-5">
        <p>&copy; 2025 Nuestra Tienda. Todos los derechos reservados.</p>
    </footer>
</body>
</html>