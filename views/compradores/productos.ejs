<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #000; /* Fondo negro */
            color: #fff; /* Texto blanco */
        }
        h1 {
            margin-bottom: 40px; /* Más separación del título */
        }
        .card {
            background-color: #1a1a1a; /* Fondo oscuro para las tarjetas */
            border: none;
        }
        .card-title, .card-text {
            color: #fff; /* Texto blanco dentro de las tarjetas */
        }
        .btn-description {
            margin-top: 10px;
        }
        
    </style>
       <!-- Barra de navegación -->
      
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Lista de Productos</h1>
        <div class="row">
            <% productos.forEach(producto => { %>
                <div class="col-md-3 mb-4">
                    <div class="card">
                        <img src="<%= producto.imagen %>" class="card-img-top" alt="<%= producto.nombre %>" style="height: 200px; object-fit: cover;">
                        <div class="card-body text-center">
                            <h5 class="card-title"><%= producto.nombre %></h5>
                            <p class="card-text">$<%= producto.precio %></p>
                            <a href="/compradores/productos/<%= producto.id %>" class="btn btn-primary btn-sm btn-description">Ver Descripción</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>